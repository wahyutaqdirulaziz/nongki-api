export interface UserInterface {
    id: number;
    email: string;
    password: string;
    name: string;
    address: string;
    gender: string; // Jika menggunakan enum, bisa diganti dengan tipe enum
    status_married: string;
  }
  